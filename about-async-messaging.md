# 비동기 메시징 시스템 도입 조건

## 1. 발표 주제

오늘 발표할 주제는 **비동기 메시징 시스템 도입 조건**입니다.

서비스 규모가 커지고 기능이 많아질수록 시스템 간 통신 방식은 점점 중요해집니다.  
처음에는 API를 직접 호출하는 동기 방식만으로도 충분하지만, 트래픽 증가, 처리 지연, 장애 전파 문제가 발생하기 시작하면 비동기 메시징 시스템 도입을 검토하게 됩니다.

비동기 메시징은 대표적으로 **Kafka, RabbitMQ, AWS SQS, Redis Stream** 같은 기술을 활용해 시스템 간 작업을 메시지 기반으로 분리하는 방식입니다.

하지만 비동기 메시징은 무조건 좋은 선택은 아닙니다.  
도입하면 구조가 복잡해지고, 메시지 유실, 중복 처리, 순서 보장, 모니터링 같은 새로운 관리 포인트가 생깁니다.

따라서 중요한 것은  
**“비동기 메시징을 언제 도입해야 하는가?”**  
그리고  
**“도입하기 전에 어떤 조건을 확인해야 하는가?”**입니다.

---

## 2. 동기 처리와 비동기 처리의 차이

먼저 동기 처리와 비동기 처리의 차이를 간단히 보겠습니다.

### 동기 처리

동기 처리는 요청을 보낸 쪽이 응답을 받을 때까지 기다리는 방식입니다.

예를 들어 사용자가 주문을 생성한다고 가정하면,

1. 주문 생성 API 호출
2. 결제 처리
3. 재고 차감
4. 알림 발송
5. 응답 반환

이 모든 과정이 하나의 흐름에서 순차적으로 처리됩니다.

동기 방식은 구조가 단순하고, 성공 또는 실패 결과를 즉시 알 수 있다는 장점이 있습니다.  
하지만 처리 시간이 길어지면 사용자 응답 속도가 느려지고, 중간 시스템 하나가 장애가 나도 전체 요청이 실패할 수 있습니다.

### 비동기 처리

반면 비동기 처리는 요청을 받은 시스템이 바로 모든 작업을 처리하지 않고, 필요한 작업을 메시지로 발행한 뒤 별도 시스템이 나중에 처리하는 방식입니다.

예를 들어 주문 생성 후,

1. 주문 생성
2. 주문 생성 이벤트 발행
3. 사용자에게 응답 반환
4. 결제, 재고, 알림은 각각 메시지를 구독한 시스템이 처리

이렇게 흐름을 분리할 수 있습니다.

즉, 비동기 처리는  
**사용자에게 즉시 필요한 작업과 나중에 처리해도 되는 작업을 분리하는 방식**이라고 볼 수 있습니다.

---

## 3. 비동기 메시징 시스템이 필요한 대표적인 상황

비동기 메시징 시스템은 다음과 같은 상황에서 도입을 검토할 수 있습니다.

---

## 4. 조건 1: 즉시 처리하지 않아도 되는 작업이 존재하는가?

첫 번째 조건은 **모든 작업이 즉시 완료될 필요가 없는가**입니다.

예를 들어 다음과 같은 작업은 사용자 요청 안에서 반드시 즉시 끝나지 않아도 되는 경우가 많습니다.

- 이메일 발송
- SMS 또는 푸시 알림
- 로그 저장
- 통계 집계
- 추천 데이터 생성
- 검색 인덱스 갱신
- 외부 시스템 연동

사용자가 회원가입을 했을 때, 회원 정보 저장은 즉시 완료되어야 합니다.  
하지만 가입 축하 이메일 발송이나 마케팅 로그 저장은 약간 늦게 처리되어도 큰 문제가 없습니다.

이런 작업까지 모두 동기 방식으로 처리하면 사용자 응답 시간이 불필요하게 길어집니다.

따라서 핵심 기준은 다음과 같습니다.

> 사용자 요청의 성공 여부와 직접 관련 없는 부가 작업이 많은가?

이 조건에 해당한다면 비동기 메시징 도입을 검토할 수 있습니다.

---

## 5. 조건 2: 트래픽이 순간적으로 몰리는가?

두 번째 조건은 **트래픽이 일정하지 않고 순간적으로 몰리는가**입니다.

서비스에서는 특정 시간대에 요청이 급증하는 경우가 있습니다.

예를 들어,

- 선착순 이벤트
- 주문 폭주
- 대량 알림 발송
- 예약 오픈
- 배치 작업 실행
- 외부 API 호출 집중

이런 상황에서 모든 요청을 즉시 처리하려고 하면 DB, 외부 API, 내부 서비스에 부하가 집중됩니다.

비동기 메시징 시스템을 사용하면 요청을 메시지 큐에 쌓아두고, 소비자가 처리 가능한 속도에 맞춰 순차적으로 처리할 수 있습니다.

즉, 메시지 큐는 일종의 **완충 장치** 역할을 합니다.

트래픽이 순간적으로 증가하더라도 시스템이 바로 무너지지 않고, 처리량을 조절하면서 안정적으로 작업을 수행할 수 있습니다.

이때 중요한 질문은 다음과 같습니다.

> 요청이 몰릴 때 모든 작업을 즉시 처리해야 하는가, 아니면 순차적으로 처리해도 되는가?

순차 처리가 가능하다면 비동기 메시징은 좋은 선택이 될 수 있습니다.

---

## 6. 조건 3: 시스템 간 결합도를 낮춰야 하는가?

세 번째 조건은 **시스템 간 결합도를 낮출 필요가 있는가**입니다.

동기 API 호출 구조에서는 한 시스템이 다른 시스템의 주소, API 스펙, 응답 형식, 장애 상황에 직접 영향을 받습니다.

예를 들어 주문 서비스가 결제 서비스, 재고 서비스, 알림 서비스를 직접 호출한다고 가정해보겠습니다.

이 경우 결제 서비스가 느려지면 주문 서비스도 느려지고, 알림 서비스가 장애가 나도 주문 처리 흐름에 영향을 줄 수 있습니다.

비동기 메시징을 사용하면 주문 서비스는 단순히  
**“주문이 생성되었다”**라는 이벤트만 발행합니다.

그 이후 결제, 재고, 알림 서비스는 각자 이 이벤트를 구독해서 필요한 작업을 처리합니다.

이 구조에서는 주문 서비스가 각 서비스의 세부 구현을 알 필요가 줄어듭니다.

즉, 비동기 메시징은 시스템 간 관계를  
**직접 호출 관계에서 이벤트 기반 관계로 전환**시켜 결합도를 낮춰줍니다.

특히 마이크로서비스 구조나 도메인별 모듈 분리가 필요한 시스템에서 효과적입니다.

---

## 7. 조건 4: 장애 전파를 줄여야 하는가?

네 번째 조건은 **일부 기능의 장애가 전체 서비스 장애로 이어지는 것을 막아야 하는가**입니다.

동기 처리에서는 중간에 호출한 외부 시스템이 장애가 나면 전체 요청이 실패할 가능성이 큽니다.

예를 들어 주문은 정상적으로 생성되었지만, 알림 서버 장애로 인해 전체 주문 요청이 실패한다면 사용자 입장에서는 이해하기 어려운 상황이 됩니다.

이때 알림 발송을 비동기로 분리하면 주문 생성은 정상 처리하고, 알림은 나중에 재시도할 수 있습니다.

비동기 메시징 시스템은 보통 다음 기능을 제공합니다.

- 실패한 메시지 재시도
- 처리 실패 메시지 별도 보관
- 소비자 장애 시 메시지 유지
- 장애 복구 후 재처리

이를 통해 일시적인 장애가 전체 서비스 실패로 확산되는 것을 줄일 수 있습니다.

핵심 기준은 다음과 같습니다.

> 부가 기능의 실패 때문에 핵심 기능까지 실패하면 안 되는가?

그렇다면 비동기 메시징을 통해 장애를 격리하는 구조를 고려할 수 있습니다.

---

## 8. 조건 5: 처리 시간이 긴 작업이 있는가?

다섯 번째 조건은 **처리 시간이 긴 작업이 존재하는가**입니다.

예를 들어 다음과 같은 작업은 요청 즉시 처리하기에는 시간이 오래 걸릴 수 있습니다.

- 대용량 파일 변환
- 이미지 또는 영상 처리
- AI 분석
- 리포트 생성
- 대량 데이터 정산
- 외부 API 반복 호출

이런 작업을 동기 방식으로 처리하면 사용자는 결과가 나올 때까지 계속 기다려야 합니다.  
또한 서버의 요청 처리 스레드가 오래 점유되어 전체 처리량이 떨어질 수 있습니다.

비동기 방식에서는 사용자의 요청을 받은 뒤 작업 요청 메시지를 큐에 넣고, 별도 워커가 작업을 수행합니다.

사용자에게는  
**“요청이 접수되었습니다”**  
또는  
**“처리 중입니다”**  
같은 상태를 먼저 응답할 수 있습니다.

이 방식은 특히 처리 시간이 예측하기 어렵거나, 작업량이 큰 기능에 적합합니다.

---

## 9. 조건 6: 이벤트 기반 확장이 필요한가?

여섯 번째 조건은 **하나의 사건에 대해 여러 후속 작업이 계속 추가될 가능성이 있는가**입니다.

예를 들어 “회원가입 완료”라는 이벤트가 발생했을 때 처음에는 이메일 발송만 필요할 수 있습니다.

하지만 서비스가 커지면 이후 다음 작업들이 추가될 수 있습니다.

- 가입 축하 쿠폰 지급
- 추천인 보상 지급
- CRM 시스템 연동
- 관리자 통계 반영
- 마케팅 자동화 시스템 연동

동기 방식이라면 회원가입 로직 안에 이런 후속 작업 호출 코드가 계속 추가됩니다.  
그러면 핵심 로직이 복잡해지고 유지보수가 어려워집니다.

반면 이벤트 기반 구조에서는 회원 서비스가 “회원가입 완료” 이벤트만 발행하고, 필요한 기능들이 각자 이벤트를 구독하면 됩니다.

즉, 새로운 기능이 추가되어도 기존 핵심 로직을 크게 수정하지 않고 확장할 수 있습니다.

이 조건은 특히 장기적으로 기능 확장이 예상되는 서비스에서 중요합니다.

---

## 10. 도입 전 반드시 확인해야 할 조건

지금까지는 비동기 메시징을 도입하면 좋은 상황을 봤습니다.  
하지만 실제 도입 전에는 반드시 확인해야 할 조건도 있습니다.

---

## 11. 확인 조건 1: 데이터 일관성 문제를 감당할 수 있는가?

비동기 처리에서는 모든 작업이 동시에 완료되지 않습니다.

예를 들어 주문은 생성되었지만, 재고 차감은 아직 처리 중일 수 있습니다.  
또는 알림 발송은 실패해서 재시도 중일 수 있습니다.

즉, 시스템 상태가 일시적으로 불일치할 수 있습니다.

이를 **최종적 일관성**이라고 합니다.

비동기 메시징을 도입하려면 서비스가 이런 상태를 허용할 수 있어야 합니다.

예를 들어,

- 결제 완료 전까지 주문 상태를 `결제 대기`로 둘 수 있는가?
- 알림 발송 실패 시 재시도 상태를 관리할 수 있는가?
- 검색 인덱스 반영이 몇 초 늦어져도 되는가?
- 통계 데이터가 실시간이 아니라 약간 지연되어도 되는가?

이런 질문에 대한 답이 필요합니다.

반대로 은행 이체처럼 즉시 강한 일관성이 필요한 작업은 단순히 메시지 큐로 분리하기 어렵습니다.

---

## 12. 확인 조건 2: 중복 처리에 대비되어 있는가?

메시징 시스템에서는 같은 메시지가 두 번 전달될 수 있습니다.

네트워크 문제, 소비자 장애, 재시도 로직 때문에 중복 처리가 발생할 수 있습니다.

따라서 비동기 메시징에서는 다음 개념이 중요합니다.

> 같은 메시지를 여러 번 처리해도 결과가 달라지지 않게 만드는 것

이를 **멱등성**이라고 합니다.

예를 들어 쿠폰 지급 메시지가 두 번 처리되면 쿠폰이 두 장 지급될 수 있습니다.  
이를 막기 위해서는 이미 처리한 메시지인지 확인하거나, 고유한 이벤트 ID를 저장해야 합니다.

중복 처리 방지를 위해 다음 방법을 사용할 수 있습니다.

- 메시지 ID 저장
- 처리 이력 테이블 관리
- 유니크 제약 조건 사용
- 상태 기반 처리
- 동일 요청 재처리 방지 로직

비동기 메시징을 도입하려면 반드시 “중복 메시지가 와도 안전한가?”를 확인해야 합니다.

---

## 13. 확인 조건 3: 메시지 순서가 중요한가?

세 번째 확인 조건은 **메시지 처리 순서**입니다.

일부 작업은 순서가 매우 중요합니다.

예를 들어 주문 상태가 다음 순서로 변경된다고 가정해보겠습니다.

1. 주문 생성
2. 결제 완료
3. 배송 시작
4. 배송 완료

그런데 메시지가 비동기로 처리되면서 “배송 시작”이 “결제 완료”보다 먼저 처리되면 데이터가 이상해질 수 있습니다.

따라서 순서가 중요한 이벤트라면 메시지 키, 파티션, 상태 검증 로직 등을 고려해야 합니다.

Kafka의 경우 같은 키를 가진 메시지를 같은 파티션으로 보내면 순서 보장에 유리합니다.  
RabbitMQ에서도 큐 설계와 소비자 수에 따라 순서가 달라질 수 있습니다.

중요한 것은 기술 자체보다  
**우리 서비스에서 순서가 깨졌을 때 문제가 되는 흐름이 무엇인지 파악하는 것**입니다.

---

## 14. 확인 조건 4: 모니터링과 장애 대응 체계가 있는가?

비동기 시스템은 눈에 보이는 흐름이 동기 API보다 복잡합니다.

API 호출은 실패하면 바로 에러를 확인할 수 있지만, 비동기 메시지는 큐에 쌓여 있거나 소비자에서 실패할 수 있습니다.

따라서 다음 항목을 모니터링해야 합니다.

- 메시지 큐 적재량
- 소비 속도
- 처리 실패 수
- 재시도 횟수
- Dead Letter Queue 메시지 수
- 소비자 서버 상태
- 메시지 처리 지연 시간

특히 Dead Letter Queue, 즉 DLQ는 처리에 계속 실패한 메시지를 따로 보관하는 큐입니다.

DLQ를 제대로 관리하지 않으면 실패한 메시지가 조용히 쌓이고, 나중에 데이터 누락 문제로 발견될 수 있습니다.

따라서 비동기 메시징 도입은 단순히 큐를 붙이는 것이 아니라,  
**운영과 관측 체계까지 함께 설계하는 것**이 중요합니다.

---

## 15. 확인 조건 5: 팀이 복잡도를 감당할 수 있는가?

비동기 메시징은 시스템을 안정적으로 확장하는 데 도움이 되지만, 개발 난이도와 운영 복잡도를 높입니다.

기존 동기 방식에서는 요청 흐름을 따라가면 문제를 찾기 쉽습니다.  
하지만 비동기 방식에서는 메시지가 어디서 발행되었고, 어떤 소비자가 처리했으며, 실패했는지 추적해야 합니다.

또한 개발자는 다음 개념을 이해해야 합니다.

- Producer와 Consumer
- Topic과 Queue
- Ack와 Retry
- Dead Letter Queue
- Message Ordering
- Idempotency
- Eventual Consistency
- Transaction Outbox Pattern

팀이 이런 개념을 충분히 이해하지 못한 상태에서 도입하면, 오히려 장애 원인을 찾기 어려운 시스템이 될 수 있습니다.

따라서 도입 전에는 기술 선택뿐 아니라 팀의 운영 역량도 함께 고려해야 합니다.

---

## 16. 도입하면 좋은 경우 정리

비동기 메시징 시스템은 다음 조건에 해당할 때 도입 가치가 큽니다.

- 즉시 처리하지 않아도 되는 작업이 많다
- 트래픽이 순간적으로 몰린다
- 시스템 간 결합도를 낮추고 싶다
- 일부 기능 장애가 전체 장애로 번지면 안 된다
- 처리 시간이 긴 작업이 있다
- 하나의 이벤트에 여러 후속 작업이 붙는다
- 이벤트 기반 확장이 필요하다

이런 상황에서는 비동기 메시징을 통해 응답 속도 개선, 장애 격리, 확장성 향상, 시스템 결합도 감소 효과를 기대할 수 있습니다.

---

## 17. 도입을 신중히 해야 하는 경우

반대로 다음과 같은 경우에는 도입을 신중히 해야 합니다.

- 현재 시스템 규모가 작고 동기 처리만으로 충분하다
- 모든 작업의 즉시 성공 여부가 중요하다
- 데이터 일관성 요구사항이 매우 강하다
- 중복 처리나 순서 보장에 대한 대비가 없다
- 모니터링과 장애 대응 체계가 없다
- 팀이 메시징 시스템 운영 경험이 부족하다

이런 상황에서 무리하게 도입하면 단순했던 구조가 오히려 복잡해지고, 장애 원인 파악이 어려워질 수 있습니다.

즉, 비동기 메시징은 성능을 올려주는 만능 도구가 아니라  
**복잡한 흐름을 분리하고 안정적으로 처리하기 위한 구조적 선택**입니다.

---

## 18. 예시: 주문 시스템에 적용하기

간단한 예시로 주문 시스템을 생각해보겠습니다.

### 동기 방식

주문 API 안에서 다음 작업을 모두 처리합니다.

1. 주문 생성
2. 결제 요청
3. 재고 차감
4. 쿠폰 사용 처리
5. 알림 발송
6. 통계 반영

이 구조에서는 알림 서버나 통계 서버가 느려져도 주문 응답이 느려질 수 있습니다.

### 비동기 방식

주문 서비스는 주문 생성 후 `OrderCreated` 이벤트를 발행합니다.

그 후 각각의 서비스가 이벤트를 구독합니다.

- 결제 서비스: 결제 처리
- 재고 서비스: 재고 차감
- 알림 서비스: 주문 완료 알림 발송
- 통계 서비스: 주문 데이터 집계
- 쿠폰 서비스: 쿠폰 사용 처리

이렇게 하면 주문 서비스는 핵심 책임에 집중할 수 있고, 후속 작업은 독립적으로 확장할 수 있습니다.

다만 이 구조에서는 각 작업의 성공 여부, 재시도, 중복 처리, 실패 메시지 관리가 반드시 필요합니다.

---

## 19. 결론

비동기 메시징 시스템은 서비스의 확장성과 안정성을 높이는 강력한 방법입니다.

하지만 도입 기준 없이 사용하면 오히려 시스템 복잡도를 크게 높일 수 있습니다.

따라서 도입 여부를 판단할 때는 다음 질문을 기준으로 보면 좋습니다.

> 이 작업은 반드시 지금 즉시 처리되어야 하는가?  
> 실패했을 때 재시도해도 되는가?  
> 시스템 간 결합도를 낮출 필요가 있는가?  
> 트래픽을 완충할 필요가 있는가?  
> 데이터의 일시적 불일치를 허용할 수 있는가?  
> 중복 처리와 장애 대응을 설계할 수 있는가?

이 질문들에 대한 답이 명확할 때 비동기 메시징 시스템 도입은 효과적인 선택이 될 수 있습니다.

결론적으로 비동기 메시징은  
**빠르게 처리하기 위한 기술이라기보다, 안정적으로 분리하고 확장하기 위한 아키텍처 전략**이라고 정리할 수 있습니다.

---

## 20. 마무리 멘트

지금까지 비동기 메시징 시스템을 도입해야 하는 조건과, 도입 전 반드시 확인해야 할 사항에 대해 설명드렸습니다.

핵심은 단순히 Kafka나 RabbitMQ 같은 기술을 사용하는 것이 아니라,  
우리 시스템의 작업 흐름과 장애 허용 범위, 데이터 일관성 요구사항을 먼저 분석하는 것입니다.

감사합니다.